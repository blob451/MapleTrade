# Django Settings
DEBUG=True
SECRET_KEY=your-secret-key-here-change-this-in-production
ALLOWED_HOSTS=localhost,127.0.0.1

# Database Settings (Docker PostgreSQL)
DB_NAME=mapletrade_db
DB_USER=postgres
DB_PASSWORD=mapletrade123
DB_HOST=localhost
DB_PORT=5432

# Database Settings (Local SQLite - currently active)
USE_SQLITE=True

# Redis Settings (Docker Redis)
REDIS_URL=redis://localhost:6379/0

# API Settings
YAHOO_FINANCE_TIMEOUT=30
DEFAULT_CACHE_TIMEOUT=3600

# Docker specific settings
POSTGRES_CONTAINER=mapletrade_postgres
REDIS_CONTAINER=mapletrade_redis